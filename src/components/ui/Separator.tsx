const Separator = () => {
  return <div style={styles.separator}></div>;
};

export default Separator;

const styles = {
  separator: {
    width: '100%',
    height: '1px',
    color: `var(--primary-white-color)`,
  },
};
